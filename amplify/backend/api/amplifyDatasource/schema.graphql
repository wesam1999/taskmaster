input AMPLIFY { globalAuthRule: AuthRule = { allow: public } } # FOR TESTING ONLY!
type Task @model {
  id: ID
  title: String
  body: String
  status: String
  TeamTask: String
}
type team @model{
  id: ID
  name:String
  listTasks : [Task] @hasMany
listUser:[user] @hasMany

}
type user @model{
  id: ID
   userName:String
  TeamUser: String
}

